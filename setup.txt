# Make a directory and move the model and tokenizer in that directory 
mkdir hybrid-Waf

# Create a fresh, pristine virtual environment
python3 -m venv waf_env

# ACTIVATE IT FIRST (Crucial step!)
source waf_env/bin/activate

# Install the dependencies cleanly into the folder
pip install --upgrade pip
pip install tensorflow numpy pandas scikit-learn shap stix2
